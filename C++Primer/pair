头文件 utility 中
eg：
    pair<string,string> p;
    or: pair<string,string> p{"aaa","bbb"};

    cout<<p.first<<p.second

pair<T1,T2> p;
pair<T1,T2> p{v1,v2}; 等价于 pair<T1,T2> P = {v1,v2};
make_pair(v1,v2); 返回一个用v1和v2初始化的pair，类型自动推断
p1 relop p2    关系运算符（<,>,<=,>=）按字典序定义
    eg:
    p1.first < p2.first 或 !(p2.first<p1.first) && (p1.second < p2.second) 成立时，
    p1 < p2 为 true
p1 == p2 当first和second都相等时成立

函数：
    eg: pair<int,string> proc(vector<int> &v)
    {
        ......
    }